---
title: 'NRSG 741: HW_01'
author: "TommY"
date: "1/26/2018"
output:
  word_document: default
  pdf_document: default
  html_document: default
---
Click link for [GitHub Repository](https://github.com/tommyflynn/N741_Homework.git)
```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
library(gapminder)
library(ggplot2)
```
# Task One:
The mean life expectancy is `r round(mean(gapminder$lifeExp), digits = 2)` years,
with a standard deviation of `r round(sd(gapminder$lifeExp), digits = 2)`, median of `r round(median(gapminder$lifeExp), digits = 2)`, and sample size of `r length(gapminder$lifeExp)`.


#Task Two:
```{r, echo = FALSE}
library(knitr)
library(dplyr)
gapminder %>% 
  group_by(continent) %>%
  summarise(LE_median = median(lifeExp), LE_sd = sd(lifeExp), LE_mean = mean(lifeExp))
```



**Citation**: Jennifer Bryan (2017). gapminder: Data from Gapminder. R package version 0.3.0.
https://CRAN.R-project.org/package=gapminder